[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings

markers =
    smoke: Fast sanity checks
    regression: Full regression suite
    splash: Splash screen tests
    home: Home screen tests
    search: Search screen tests

log_cli = true
log_cli_level = INFO
